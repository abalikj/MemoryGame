<Window x:Class="MemoryGameWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Memory Game" Height="600" Width="600"
        MinHeight="400" MinWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TurnLabel" FontSize="18" Margin="10" Text="На потег е: Ти"/>
        <TextBlock x:Name="TimerText" FontSize="16" Margin="10" Grid.Row="1"/>

        <Viewbox Grid.Row="2" Stretch="Uniform">
            <UniformGrid x:Name="CardGrid"/>
        </Viewbox>
    </Grid>
</Window>